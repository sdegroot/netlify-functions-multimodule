{
  "name": "web",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/ dist/ && ls -l dist"
  },
  "dependencies": {
    "api": "1.0.0"
  },
  "devDependencies": {
    "http-server": "^0.12.3"
  }
}
